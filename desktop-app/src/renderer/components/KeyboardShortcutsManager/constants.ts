// The shortcut table is shared with the main process, which matches raw key
// events coming from preview webviews (see common/shortcuts.ts).
export {SHORTCUT_CHANNEL, SHORTCUT_KEYS} from 'common/shortcuts';
export type {ShortcutChannel} from 'common/shortcuts';
